# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Runtime SQLite database (accounts) — persisted via a Docker volume in
# production, never committed.
/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Печатные листы — сборочный результат другой работы. В репозиторий не идут:
# один такой PDF весит под 140 МБ, GitHub отбивает пуш на лимите в 100 МБ, а
# лежит он в public/, то есть Vite тащил его ещё и в production-образ.
apps/web/public/assets/output/
